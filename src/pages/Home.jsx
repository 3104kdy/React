// QueryStrings 사용 방법
import { useSearchParams } from "react-router-dom";


const Home = () => {
    // 쿼리 스트링으로 전달된 파라미터를 가져올 수 있음
    // const [params, setParams] = useSearchParams();
    return <div> Home</div>;
}

export default Home;